package com.mining.graphics.graphics.support;

import com.mining.graphics.model.support.ModelShotcreteExcavation;

import java.awt.*;

public class GraphicsShotcreteEx extends ModelShotcreteExcavation {
    public void paint(Graphics g) {

        //Левая стенка
        //g.drawLine(0, Hsc, 0, hrsc);
        //Набрызгбетон в кровле
//        Color shotcrete = new Color(200, 200, 200);
//        g.setColor(shotcrete);
//        g.drawArc(0 + tshsc, hrsc - rsc + tshsc, 2 * (rsc - tshsc), 2 * (rsc - tshsc), (int) (90 + (alpha * 180 / Math.PI)), (int) (beta * 180 / Math.PI));
//        g.drawArc((int) ((B / 2 - R + tsh) * scaleEx), 0 + tshsc, 2 * (Rsc - tshsc), 2 * (Rsc - tshsc), (int) (beta * 180 / Math.PI), (int) (2.0 * alpha * 180 / Math.PI));
//        g.drawArc((int) ((B - 2 * r + tsh) * scaleEx), hrsc - rsc + tshsc, 2 * (rsc - tshsc), 2 * (rsc - tshsc), 0, (int) (beta * 180 / Math.PI));
        //Правая стенка
        //g.drawLine(Bsc, hrsc, Bsc, Hsc);

        //Вид сбоку

    }
}
